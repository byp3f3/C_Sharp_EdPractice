<Window x:Class="Pr3.MainWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pr3"
        mc:Ignorable="d"
        Loaded="Window_Loaded"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <!--EF-->
        <!--<DataGrid x:Name="humanDgr" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding ID_Human}" Header="ID"/>
                <DataGridTextColumn Binding="{Binding Surname}" Header="Фамилия"/>
                <DataGridTextColumn Binding="{Binding FirstName}" Header="Имя"/>
                <DataGridTextColumn Binding="{Binding Middle_name}" Header="Отчество"/>
                <DataGridTextColumn Binding="{Binding Book.Book_name}" Header="Любимая книга"/>
            </DataGrid.Columns>
        </DataGrid>-->
        
        <!--DataSet-->
        <DataGrid x:Name="humanDgr" CanUserAddRows="False" />
    </Grid>
</Window>
